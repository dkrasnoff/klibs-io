klibs:
  indexing: false
  ai: false
  scheduling:
    process-indexing-queue:
      enabled: false

spring:
  sql:
    init:
      mode: always
  cloud:
    aws:
      region:
        static: eu-west-1
      credentials:
        access-key: test
        secret-key: test
      endpoint: http://localhost:4566
#  profiles:
#    active: test